name: Test - mesoscope

on:
  pull_request:
    branches:
      - main
    paths:
      - 'src/aind_metadata_extractor/mesoscope/**'
      - 'src/aind_metadata_extractor/models/mesoscope.py'
      - 'tests/mesoscope/**'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    steps:
    - uses: actions/checkout@v5
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: python -m pip install -e .[dev,mesoscope]
    - name: Run tests and coverage
      run: |
        coverage run --source=src/aind_metadata_extractor/mesoscope -m unittest discover -s tests/mesoscope -p 'test_*.py'
        coverage report
